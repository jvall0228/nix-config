Read `/etc/agent-context.md` to get pre-computed system context (hostname, NixOS version, GPU, packages, configuration paths, constraints).

Then run `bash apps/system-status` for a full system health check including the context file, system health, and current generation.
